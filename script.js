function submit(str) {
    const code = "xlccpbfd"
    if(str != null) {
        if(str.toLowerCase() == code) {
            window.location.href = "fortune-wheel.html"
        } else {
            //falscher code
        }
    }
}